<div class="close-out"><%= link_to "x", "#" %></div>
  <div class="result-modal">
  <p>
    <h2 class="ailment-recipe">Here is your smoothie:</h2>
    <h2><%= link_to @recipe_name.upcase, "http://www.wikipedia.org/#{@disease_url}", :target => "blank" %></h2>
  </p>
  <%= image_tag "skull-and-crossbones.png", :id => "skull-image" %>
  <p class="disclaimer"><strong>Disclaimer:</strong><br>
    Nutrition is a supplement, not a substitute, for conventional medicine.  Please consider food allergies, carb/fruit sugar content, medicine contraindications, etc. before consuming new foods.</p>
  <h3 class="try-again-link"><%= link_to "Meh? Try again for a new recipe >>", "#" %></h3>
  </div>







<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
